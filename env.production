# Production Environment Variables for Heroku
# These will be set via Heroku config vars, this file is for reference

# Database (automatically set by Heroku Postgres addon)
DATABASE_URL="${DATABASE_URL}"

# Redis (automatically set by Heroku Redis addon)  
REDIS_URL="${REDIS_URL}"

# Node Environment
NODE_ENV="production"
LOG_LEVEL="info"

# Port (automatically set by Heroku)
PORT="${PORT}"

# Auth (Clerk) - Set these in Heroku config
CLERK_PUBLISHABLE_KEY="${CLERK_PUBLISHABLE_KEY}"
CLERK_SECRET_KEY="${CLERK_SECRET_KEY}"
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="${NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY}"

# API Configuration
WEBHOOK_TRACKSTAR_SECRET="${WEBHOOK_TRACKSTAR_SECRET}"

# S3/R2 Configuration - Set these in Heroku config
S3_BUCKET="${S3_BUCKET}"
S3_REGION="${S3_REGION}"
S3_ACCESS_KEY_ID="${S3_ACCESS_KEY_ID}"
S3_SECRET_ACCESS_KEY="${S3_SECRET_ACCESS_KEY}"

# Optional: Error Tracking & Analytics
SENTRY_DSN="${SENTRY_DSN}"
POSTHOG_KEY="${POSTHOG_KEY}"
NEXT_PUBLIC_POSTHOG_KEY="${NEXT_PUBLIC_POSTHOG_KEY}"

# API Base URL (set to your Heroku app URL)
NEXT_PUBLIC_API_BASE="https://your-app-name.herokuapp.com"
